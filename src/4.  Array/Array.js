"use strict";
/*
Array can typed using a type annotation followed by empty array brackets, like number[]
for an Array of numbers
*/
// empty array
var emptyArray = [];

// any array
var newArray = [];

// string Array 
var names = ['hello', 'world'];

// number array
var ages = [122, 233, 4563, 313];
// 
var bools = [];

// AlterNative Syntax
var Myname = ['hello', 'world'];

var myAge = [12, 34, 454, 546];
//note : These array only allow data of that one type inside them
/*
================================
     Empty Array
================================
 */
var activeUser = []; // this is empty array i can not put values in it
var coords = [];
coords.push({ x: 23, y: 8 });
/*
================================
     multidimensial Array
================================
 */
// two dimensial rray
var board = [["X", "O", "X"], ["X", "O", "X"], ["X", "O", "X"]];
//  we can also create more dimensial array if we need
